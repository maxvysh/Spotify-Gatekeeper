// const background = document.querySelector('.background');

// document.addEventListener('mousemove', (e) => {
//   const { clientX, clientY } = e;
//   const screenWidth = window.innerWidth;
//   const screenHeight = window.innerHeight;

//   const xPercent = (clientX / screenWidth) * 100;
//   const yPercent = (clientY / screenHeight) * 100;

//   const color1 = `hsl(${xPercent * 3}, 100%, 50%)`;
//   const color2 = `hsl(${yPercent * 3}, 100%, 50%)`;

//   background.style.background = `linear-gradient(45deg, ${color1}, ${color2})`;
// });